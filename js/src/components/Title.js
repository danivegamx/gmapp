var React = require('react');

var Title = React.createClass({

	/*
	*	TODO React-required;
	*	Method to render the component.
	*
	*	@return jsxViewController
	*/
	render() {
		return (
			<div className="title">Google Maps React.js app</div>
		);
	}
});

module.exports = Title;
