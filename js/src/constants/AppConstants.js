var keyMirror = require('keymirror');

module.exports = keyMirror({
	SEARCH_ADDRESS: null,				// Search a specific address.
	CHECK_IF_ITS_ON_FAVORITES: null,	// Check if the address is on favorites.
	ADD_TO_FAVORITES: null,				// Add address to favorite list.
	REMOVE_FROM_FAVORITES: null,		// Remove address from favorite list.
});
